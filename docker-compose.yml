services:
  db-postgres:
    image: postgres:17.0
    restart: always
    environment:
      POSTGRES_USER: gestion_gastos_admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: gestion_gastos
    ports:
      - '5432:5432'